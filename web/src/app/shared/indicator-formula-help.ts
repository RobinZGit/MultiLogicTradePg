/** Краткая подсказка под полем формулы (create / edit). */
export const INDICATOR_FORMULA_HINT =
  'Многочлен по барам: pp (close), oo/hh/ll/vv, ядро (1; -2; 1), * — свёртка, # — покомponentное умножение, @RSI — другой индикатор, sma(pp) / ema(pp) / ww().';

/** Развёрнутая справка по кнопке «И.» */
export const INDICATOR_FORMULA_HELP_DETAIL = `Многочленная формула — это выражение над числовыми рядами (массивами по барам). Результат — одна линия индикатора на графике.

Рыночные ряды
  pp — цена закрытия (Close)
  oo, hh, ll, vv — Open, High, Low, Volume

Ядра (коэффициенты фильтра)
  (a; b; c) или (a, b, c) — вектор; точка с запятой и запятая эквивалентны
  Пример: pp * (1; -2; 1) — вторая разность цены (ускорение, PACC)

Операции между рядами
  * — свёртка (скользящий фильтр по окну)
  # — покомponentное умножение
  /# — покомponentное деление
  +, − — покомponentное сложение и вычитание

Функции
  sma(expr) — простое скользящее среднее от expr (период из параметров серии)
  ema(expr) — экспоненциальное среднее
  ww() — только ядро SMA без аргумента (удобно для цепочек: pp * ww() * ww())

Ссылки на другие индикаторы
  @SMA, @RSI, @MACD:HISTOGRAM — готовый ряд другого индикатора

Примеры
  sma(pp)                    — SMA по close
  sma(sma(sma(pp)))          — тройное SMA (SMAT3)
  pp * ww() * ww() * ww()    — то же SMAT3, другая запись
  @SMA # pp                  — SMA × цена покомponentно
  sma(pp) - sma(oo)          — разность двух свёрток

При добавлении на бумагу серия VALUE создаётся автоматически; расчёт идёт через calc_poly_formula_array в PostgreSQL.`;
